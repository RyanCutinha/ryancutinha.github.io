source "https://rubygems.org"

# If you're using a specific Jekyll theme gem directly
# gem "jekyll-theme-slate" 

# OR, and this is the most common and recommended way for GitHub Pages:
# Use the github-pages gem, which includes Jekyll and all supported plugins.
gem "github-pages", group: :jekyll_plugins

# To run Jekyll locally on Windows, you might also need `webrick`
gem "webrick", "~> 1.7" # Add this if you encounter errors running locally on Windows
